import type { Metadata, Viewport } from "next";
import { Geist, Geist_Mono } from "next/font/google";
import "./globals.css";

const geistSans = Geist({
  variable: "--font-geist-sans",
  subsets: ["latin"],
});

const geistMono = Geist_Mono({
  variable: "--font-geist-mono",
  subsets: ["latin"],
});

export const metadata: Metadata = {
  title: "Prasad Invoice | Modern Management System",
  description: "Professional Invoice, Customer & Product Management System for Prasad Cold Coco.",
  icons: {
    icon: "/logo-chocolate-nobg.png",
    apple: "/logo-chocolate-nobg.png",
  },
  keywords: ["invoice", "management", "billing", "prasad cold coco", "surat"],
  authors: [{ name: "Prasad Cold Coco" }],
};

export const viewport: Viewport = {
  width: "device-width",
  initialScale: 1,
};

export default function RootLayout({
  children,
}: Readonly<{
  children: React.ReactNode;
}>) {
  return (
    <html
      lang="en"
      className={`${geistSans.variable} ${geistMono.variable} h-full antialiased`}
      suppressHydrationWarning
    >
      <body className="min-h-full flex flex-col" suppressHydrationWarning>
        {children}
      </body>
    </html>
  );
}
